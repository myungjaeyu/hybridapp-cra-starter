# hybridapp-cra-starter

## Usage

```
$ npm i

$ npm start
```

### Hybrid App

```bash
$ npm i -g cordova

$ cordova platform add browser
# $ cordova platform add android
# $ cordova platform add ios

# $ rm -r www
$ npm run build:cordova

$ cordova run browser
```